import { colors } from "./colors";
import { space, lineHeights, letterSpacing } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  letterSpacing,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
